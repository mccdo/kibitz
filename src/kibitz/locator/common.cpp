#include <kibitz/locator/common.hpp>


namespace kibitz {
  namespace locator {
    void get_bindings( const worker_binding_t& binding_root, 
		       const worker_graph& graph, 
		       int base_port, 
		       binding_map_t& bindings ) {
    }
  }
}
